open your terminal and run the following commands
1. create virtual environment 
    1.1 virtualenv venv
    1.2 source venv/bin/activate

2. install django and django-channels
    2.1 pip install django
    2.2 pip install -U channels["daphne"]

3. run the server
    3.1 python manage.py runserver

